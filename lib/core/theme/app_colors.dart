import 'package:flutter/material.dart';

class AppColors {
  static const seed = Color(0xFF1B8D5A);

  static const primary = Color(0xFF2DD4BF);
  static const background = Color(0xFFF0FDFA);

  static const text = Color(0xFF0F172A);
  static const subText = Color(0xFF64748B);
}
